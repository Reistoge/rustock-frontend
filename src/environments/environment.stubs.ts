export const environment = {
  apiBaseUrl: 'http://localhost:3000',
  // Selected with `ng serve --configuration=stubs` (`npm run start:stubs`,
  // see USE_STUBS in `.env.example`). Replaces the HTTP APIs with the
  // in-memory stubs in `src/app/core/api/stubs/`.
  useStubs: true,
};
